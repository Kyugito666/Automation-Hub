# ============================================
# AUTOMATION-HUB (Codespace Architecture) GITIGNORE
# ============================================

# --- CRITICAL: Runtime & State ---
# Abaikan SEMUA yang di-clone/dibuat saat runtime di remote
# TAPI JANGAN abaikan skrip deployer itu sendiri
bots/*
!bots/.gitkeep

# Abaikan folder proxysync (jika dikelola git terpisah, jika tidak, hapus baris ini)
# proxysync/ 

# File state & cache dari Orchestrator TUI (LOKAL)
.token-state.json
.token-cache.json

# Log remote
startup.log

# --- Python Cache & Venv ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
myenv/

# --- Node Modules ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- .NET Build Artifacts ---
**/bin/
**/obj/

# --- General Logs ---
*.log
*.log.*
logs/
*.log

# --- Environment & Secrets ---
# (Meskipun config utama di-commit, .env lokal JANGAN)
.env
.env.*
*.env

# --- IDE & Editor Files ---
.vscode/
.idea/
*.suo
*.user
*.sln.docstates
*.sw? # Vim swap files

# --- OS Generated Files ---
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# --- Temporary & Backup Files ---
*.tmp
*.temp
*.bak
*.backup
*.old

# --- File yang TIDAK DIPAKAI lagi ---
# .bot-inputs/
# .encryption-key
# ... (Semua isi .gitignore lu yang lama) ...

# --- Environment & Secrets ---
# (Meskipun config utama di-commit, .env lokal JANGAN)
.env
.env.*
*.env
config/apikeys.txt # <-- TAMBAHKAN BARIS INI
